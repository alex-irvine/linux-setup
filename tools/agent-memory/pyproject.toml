[project]
name = "agent-memory"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
memoryctl = "agent_memory.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]

[dependency-groups]
dev = ["pytest>=8.0"]
